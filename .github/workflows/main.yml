# name: Deploy to EC2

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v3

#       - name: Setup SSH
#         run: |
#           mkdir -p ~/.ssh
#           echo "${{ secrets.EC2_KEY }}" > ~/.ssh/id_ed25519
#           chmod 600 ~/.ssh/id_ed25519
#           ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts

#       - name: Deploy over SSH
#         run: |
#           ssh ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} << 'EOF'
#             set -e
#             cd ~/your-repo-folder

#             echo "Pulling latest code..."
#             git pull origin main

#             echo "Checking ElizaOS changes..."
#             if git diff HEAD@{1} HEAD --name-only | grep -q "elizaos/package.json\|elizaos/pnpm-lock.yaml"; then
#               echo "Dependencies changed. Installing..."
#               cd elizaos
#               pnpm install
#             fi

#             echo "Building frontend..."
#             cd ~/your-repo-folder/elizaos
#             pnpm build
#             pm2 restart elizaos || pnpm run start &

#             echo "Restarting Django..."
#             cd ~/your-repo-folder/backend
#             source venv/bin/activate
#             python manage.py migrate
#             systemctl restart gunicorn
#             systemctl restart nginx
#           EOF
